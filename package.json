{
  "name": "project",
  "version": "1.0.0",
  "description": "Express + vanilla frontend web app",
  "main": "server.js",
  "type": "module",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "start": "node --env-file=.env server.js",
    "dev": "node --env-file=.env --watch server.js",
    "build": "node --check server.js && node --check netlify/functions/api.js && node --check lib/auth.js && node --check lib/supabase.js && node --check lib/groq.js && node --check lib/store.js && node --check lib/gov-engine.cjs && node --check lib/gov-policy-db.cjs && node --check public/government.js"
  },
  "license": "ISC",
  "dependencies": {
    "express": "^5.2.1",
    "serverless-http": "^4.0.0"
  }
}
